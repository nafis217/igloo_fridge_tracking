services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: iglootrack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - iglootrack_data:/var/lib/postgresql/data

volumes:
  iglootrack_data:
